import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';
import {GitFork,Hexagon,LockKeyhole,Plus,Sparkles,Tag} from 'lucide-react';
import {useMemo,useState} from 'react';
import {api,writeApi} from '../api';
import type {TaxonomyResponse} from '../types';

export function TaxonomyPage(){const queryClient=useQueryClient();const[name,setName]=useState('');const[parent,setParent]=useState('');const{data}=useQuery({queryKey:['taxonomy'],queryFn:()=>api<TaxonomyResponse>('/api/taxonomy')});const families=useMemo(()=>data?.main.map(main=>({...main,children:data.sub.filter(sub=>sub.parent_id===main.id)}))??[],[data]);const custom=data?.patterns.filter(node=>node.kind==='custom')??[];const create=useMutation({mutationFn:()=>writeApi('/api/taxonomy','POST',{name,parent_id:parent||null,aliases:[]}),onSuccess:()=>{setName('');setParent('');queryClient.invalidateQueries({queryKey:['taxonomy']})}});return <section className="page-scroll"><div className="page-heading"><div><p className="eyebrow"><Hexagon size={13}/> CLASSIFICATION SYSTEM</p><h2>Algorithm taxonomy</h2><p>One stable primary path, unlimited cross-cutting technique signals.</p></div><div className="taxonomy-legend"><span><LockKeyhole size={11}/> CURATED CORE</span><span><Tag size={11}/> CUSTOM SIGNALS</span></div></div><div className="taxonomy-stats"><article className="panel"><strong>{data?.main.length??0}</strong><span>MAIN FAMILIES</span></article><article className="panel"><strong>{data?.sub.length??0}</strong><span>SUB-TAGS</span></article><article className="panel"><strong>{data?.patterns.filter(n=>n.kind==='pattern').length??0}</strong><span>TECHNIQUES</span></article><article className="panel"><strong>{data?.failure_reasons.length??0}</strong><span>FAILURE MODES</span></article></div><div className="taxonomy-layout"><div className="family-grid">{families.map((family,index)=><article className="panel family-card" key={family.id} style={{'--family':family.color??'#2de2e6','--delay':`${index*40}ms`} as React.CSSProperties}><header><i><Hexagon size={17}/></i><span><b>{family.name}</b><small>{family.children.length} sectors</small></span><LockKeyhole size={12}/></header><div>{family.children.map(child=><span key={child.id}><i/>{child.name}</span>)}</div></article>)}</div><aside><article className="panel taxonomy-side"><div className="panel-title"><span><GitFork size={14}/> PATTERN LAYER</span></div><p>Techniques connect problems across primary domains without duplicating them.</p><div className="pattern-cloud">{data?.patterns.filter(n=>n.kind==='pattern').map(node=><span key={node.id}>{node.name}</span>)}</div></article><article className="panel custom-panel"><div className="panel-title"><span><Sparkles size={14}/> CUSTOM SIGNAL</span></div><p>Add personal labels while keeping the curated hierarchy clean.</p><label><span>TAG NAME</span><input value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Revisit with recursion"/></label><label><span>OPTIONAL PARENT</span><select value={parent} onChange={e=>setParent(e.target.value)}><option value="">Cross-cutting</option>{data?.main.map(node=><option value={node.id} key={node.id}>{node.name}</option>)}</select></label><button className="primary-btn" onClick={()=>create.mutate()} disabled={!name||create.isPending}><Plus size={14}/> CREATE SIGNAL</button>{custom.length>0&&<div className="custom-list">{custom.map(node=><span key={node.id}>{node.name}</span>)}</div>}</article></aside></div></section>}
